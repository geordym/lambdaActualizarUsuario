{
  "name": "actualizarUsuarioLambda",
  "version": "1.0.0",
  "description": "Función Lambda para gestionar usuarios",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.x.x",
    "@aws-sdk/lib-dynamodb": "^3.x.x"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=14.x"
  }
}
